Include ~/.local/share/ssh/*.conf
Include ~/.ssh/1password.conf

# If ghostty isn't playing nice when SSHing there is either this command:
# infocmp -x xterm-ghostty | ssh user@host -- tic -x -
# Or add the following for the specific host:
# Host example.com
#   SetEnv TERM=xterm-256color
# Source: https://ghostty.org/docs/help/terminfo#ssh
